services:
  - postgres

stages:
  - build
  - test
  - deploy

job1:
  stage: build
  before_script:
    - ./gradlew --stop
  script:
    - ./gradlew build -x test

job2:
  stage: test
  script:
    - ./gradlew build

job3:
  stage: deploy
  script:
    - ./deployShell.sh

